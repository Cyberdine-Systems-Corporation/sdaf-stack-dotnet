# Dependencias del sitio de documentación (MkDocs + Material + mike).
# Alineado con sdaf-core/requirements-docs.txt (mismo stack de publicación versionada).
# Instalar con: pip install -r requirements-docs.txt
mkdocs==1.6.*
mkdocs-material==9.5.*
mike==2.1.*
mkdocs-git-revision-date-localized-plugin==1.2.*
pymdown-extensions==10.*
