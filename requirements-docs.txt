# Dependencias del sitio de documentación (MkDocs + Material + mike).
# Alineado con sdaf-core/requirements-docs.txt (Decisión A: mismo stack visual).
# Instalar con: pip install -r requirements-docs.txt
mkdocs==1.6.*
mkdocs-material==9.5.*
mike==2.1.*
pymdown-extensions==10.*
